---
layout: base
---

<main class="container mx-auto max-w-3xl flex flex-col flex-wrap items-center px-6 pt-24 md:pt-36">
	<p class='self-start text-grey-300 text-xs'>Last update: {{ page.date.toLocaleString() }}</p>
	<header class="flex w-full flex-row max-sm:flex-wrap items-center justify-center overflow-y-hidden mb-10">
		<div class="flex flex-col justify-center gap-2 h-full">
			<h1 class="text-left text-3xl font-bold leading-tight text-white">
				{{ metadata.title }}
			</h1>
			<p class="text-base leading-normal mr-5 md:text-left md:text-lg">{{ metadata.description }}</p>
			<div id="header-links" class="flex gap-2 text-white text-lg underline">
				<a href="#">faq</a>
				<a href="#">resources</a>
				<a href="#">about us</a>
			</div>
		</div>
		<img src="/soc.png" width="300px" alt="">
	</header>
	<div class="self-start w-full">
		{{ content | safe }}
	</div>
</main>
